# Copy to .env and fill in real values. NEVER commit .env.

# Supabase Postgres — Connect → Session pooler → URI
# Remove [brackets] around the password.
DATABASE_URL=

# Optional (frontend uses public/js/config.js or Netlify env)
SUPABASE_URL=
SUPABASE_ANON_KEY=
